/*
 * Minimal Dexed.h stub for standalone msfa compilation
 */

#ifndef DEXED_H
#define DEXED_H

#define DEXED_VERSION "1.0.0"

#ifdef DEBUG
    #define TRACE(fmt, ...)
#else
    #define TRACE(fmt, ...)
#endif

#endif
